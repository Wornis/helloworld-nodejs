apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: 6a29b83
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0 # Should be zero unavailable pods while deploying.
      maxSurge: 1 # Should be one new pod at a time.
  replicas: 1
  selector:
    matchLabels:
      app: 6a29b83
  template:
    metadata:
      labels:
        app: 6a29b83
    spec:
      containers:
        - name: 6a29b83
          image: gcr.io/ad4screen-us/6a29b83:6a29b83
          ports:
            - containerPort: 8080
              name: http
          readinessProbe: # Send an http get request to the path / every five seconds and if it is successful, mark the pod ready and start sending traffic to it.
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            successThreshold: 5
---
kind: Service
apiVersion: v1
metadata:
  name: 6a29b83-service
spec:
  selector:
    app: 6a29b83
  ports:
    - targetPort: 8080
      port: 80
      name: http
  type: LoadBalancer
  loadBalancerIP: 35.247.85.46

